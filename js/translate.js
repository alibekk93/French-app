// Translation wrapper.
// Needs: free, no payment method required, limits are acceptable.
// Candidates to evaluate: MyMemory (no key, daily cap), LibreTranslate
// (free public instances, self-hostable if limits become an issue).
// TODO: implement translate(text, targetLang) for targetLang in ['en', 'ru'].

const Translate = {
  async translate(text, targetLang) {
    // not implemented
  },
};
