// Vocab mode.
// TODO:
// - List/manage the dictionary (Dictionary.getAll / .add).
// - Manual add: word/phrase -> auto-translate (EN + RU) via Translate, store.
// - Flashcard trainer:
//   - card type setting: french-hidden | translation-hidden | mixed
//   - input type setting: write-in | multiple-choice (4 options)
//   - runs indefinitely until the user stops

const VocabMode = {
  render(container) {
    // not implemented
  },
};
