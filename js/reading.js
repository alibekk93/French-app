// Reading mode.
// TODO:
// - Accept a URL, fetch and render its text in a reading-friendly layout.
// - Accept pasted plain text as an alternative input.
// - On text selection, call Translate.translate(text) and show a popup
//   with the French original + English + Russian translations.
// - Let the user save a popup's contents into Dictionary.

const ReadingMode = {
  render(container) {
    // not implemented
  },
};
